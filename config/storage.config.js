const storage = {
  log: __dirname + '/../storage/logs',
  cache: __dirname + '/../storage/cache',
  app: __dirname + '/../storage/app',
  session: __dirname + '/../storage/session',
};

module.exports = storage;
